{
  "name": "ai-receipt-memory",
  "version": "1.0.0",
  "description": "AI-powered receipt memory application",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm install --include=dev && (chmod +x ./node_modules/.bin/vite || true) && npm run build && cd ../server && npm install",
    "start": "node server/server.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
